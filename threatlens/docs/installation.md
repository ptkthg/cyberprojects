# Installation

1. Create virtual environment.
2. Install requirements.
3. Configure `.streamlit/secrets.toml`.
4. Run `streamlit run app.py`.

For Windows desktop launcher, run `python launcher.py`.
